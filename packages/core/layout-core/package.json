{
  "name": "@poem/layout-core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "诗词排版核心 —— 导出与跨端渲染共享的排版语义、正文分组和文本生成规则",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@poem/editor-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "^3.2.4"
  }
}
