{
  "name": "@poem/agent-client",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Agent 客户端纯逻辑层 —— 上下文采集 + 请求构建 + 流式解析 + 对话状态（待开发）",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
